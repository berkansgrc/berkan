import ExamCreateForm from "@/components/admin/ExamCreateForm";

export const metadata = {
  title: "Yeni Sınav | Berkan Matematik",
};

export default function NewExamPage() {
  return <ExamCreateForm />;
}
